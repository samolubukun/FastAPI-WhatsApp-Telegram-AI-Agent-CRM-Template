name: whatsapp-agent
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
    - fastapi
    - uvicorn[standard]
    - pydantic
    - pydantic-settings
    - httpx
    - openai
    - python-dotenv
